# CLAUDE.md

> 本文件是默认长期约束，动手前先读完。

## 协作与执行

### 编码前

- 不知道项目现状时，先查看和任务直接相关的内容，不凭假设动手。
- 理解不确定时，先说明当前理解，得到确认后再继续。
- 存在多个可行方案时，先说明准备采用的方案，得到确认后再继续。

### 简洁优先

- 每一处改动都必须直接对应当前需求，不额外增加未要求的内容。
- 只实现当前已确认的需求，不为未来可能的变化预留接口、工厂、配置等扩展点。
- 只为已确认的输入、输出、异常、风险和边界写逻辑；没有依据的假设场景，不补防御分支、兜底代码和回滚逻辑（安全风险除外）。
- 同一流程按步骤衔接、且只服务这条流程的逻辑留在同一个文件，不为缩短局部实现拆到多文件；类型、常量等仍按落位规则归类。某一步被两处及以上独立流程复用时才拆出。
- 非核心业务的通用基础能力，先复用成熟组件或项目现有依赖，不自实现。

### 精准修改

- 现有的内容能满足当前需求时必须复用；写代码前先看当前目录和相邻模块已在使用的方式，能沿用就沿用。
- 不重构本来没有坏掉的实现。
- 只清理本次修改直接造成的无用内容；原本就存在的无关问题只说明，不擅自删除。

### 完成标准与验证

- 动手前先写清这次任务做成什么样算完成。
- 交付前必须用当前项目已确认的方式验证结果；没有运行验证时，必须明确说明。

## 通用代码规则

### 外部输入

- 来自接口、文件、存储、用户提交或第三方系统的内容，先检查格式、内容和必填项，通过后再处理。

### 流程与分支

- 前置条件不满足或已无需继续时，先结束这一步（卫语句提前返回），不把正常流程包进多层判断。
- 同一判断项在 5 个及以上互斥分支中选其一时，改成统一维护的分发表（取值映射到结果或处理逻辑）；分支存在先后顺序、状态推进或多个条件共同决定结果时，才保留显式分支。

### 命名一致性

- 不使用 utils、helper、manager 这类不指向具体含义的空泛词；文件名不使用只表示"通用""杂项"的泛化名称。
- 同一个概念在整个项目里固定用同一个词，不混着用。

### 人类可见文案

- 面向人阅读的日志说明文字和错误信息使用中文。
- 命令名、路径、字段名、错误码、协议名等固定标识保留原文。

## TypeScript 规则

### 注释

- 文档注释使用 JSDoc 规范的 `/** */`，不用 `//` 写文档注释。
- 不写单行 `/** 内容 */`，文档注释用独立的 `/** */` 结构。
- 单行注释放在对应代码上方，不写在语句末尾。

### 命名

变量前后缀：

| 含义 | 形式 |
| --- | --- |
| 布尔变量 | 只用 `is`（状态）、`has`（拥有关系）、`can`（能力或权限）三个前缀 |
| 数组 | 主题词加 `List`：`userList` |
| 数组元素 | 主题词加 `Item`：`userList.forEach((userItem, userIndex) => {})` |
| 数组位置 | 主题词加 `Index`，不缩写成 `idx` |
| 全局配置 | 主题词加 `Config`：`appConfig` |
| 函数参数选项 | 主题词加 `Option`：`function loadList(loadOption: LoadOption)` |

函数动词固定搭配：

| 语义 | 动词 | 不用 |
| --- | --- | --- |
| 执行入口流程 | `run` | `start` |
| 设置内存中的单个属性 | `set` | `update` |
| 持久化保存到后端或存储 | `save` | `store` |
| 从无到有产出新实例 | `create` | `build` |
| 组装、转换已有输入 | `build` | `create` |
| 移除数组元素 | `remove` | `delete` |
| 初始化实例 | `init` | `setup` |
| 事件处理 | `handle` 加事件名 | `onClick` 式命名 |

- 文件名统一小写字母加中划线：`load-detail.ts`，不用驼峰或下划线。

### 模块与语句

- 模块导出统一写在文件底部：先写声明或定义，再集中 `export`，不在声明处直接写 `export`。
- 只用作类型的导入导出用 `import type`、`export type`；一条语句里类型和值都有时拆成两条，不混写。
- 桶文件逐个具名列出对外符号（值用 `export { ... } from`，类型用 `export type { ... } from`），不用 `export *` 或 `export type *`。
- 禁止使用三目运算符。
- 禁止使用 `switch` 语句。

### 类型

- 枚举风格的值用 `as const` 对象提供运行时取值，配合联合类型约束，不用 `enum`：

  ```typescript
  const Status = {
    IDLE: "idle",
    BUSY: "busy",
  } as const

  type Status = typeof Status[keyof typeof Status]
  ```

- 描述对象字段结构用 `interface`；联合类型、字面量类型、元组、函数类型、映射类型、条件类型和类型组合的结果用 `type`。
- 参数、返回值、字段都写清具体类型，不用 `any`，也不用 `unknown` 兜底。
- 不用 `as SomeType` 类型断言；`as const` 是唯一例外。

### 函数与类

- 类属性单独在类体声明，不在 `constructor` 参数上用 `public`、`private`、`protected`、`readonly`（含组合）直接声明参数属性。
- `catch (error)` 先用 `instanceof` 类型守卫收窄，先判断更具体的子类、再判断父类；不直接用 `as` 断成具体类型。

### 目录落位

共享目录内部一律扁平，文件直接放在目录下（`service/` 除外，它只含 `request/`、`apis/` 两个子目录）：

| 内容 | 落位 |
| --- | --- |
| `interface`、`type`、取值集合式 `as const` 对象枚举 | 全局 `types/`，按主题一个文件 |
| 不随环境调整、被多个文件复用的固定 `const` 值和展示文案映射表 | 全局 `constants/` |
| 会按环境、平台或运行条件调整的配置对象 | 全局 `config/` |
| 处理数组、对象、字符串、路径、时间、格式转换等通用技术内容、被多个文件复用 | `tools/` |
| 请求客户端和传输层 | `service/request/`（即使含按环境调整的配置也不进 `config/`） |
| 只做请求地址分发和客户端调用的请求函数 | `service/apis/` |

- 每个共享目录（含 `service/`、`service/request/`、`service/apis/`）在目录根下建一个 `index.ts` 桶文件。
- 跨目录导入路径停在最近的桶文件：`@/tools`、`@/service/request`，不深入到具体文件，也不绕过桶文件。
- 文件名写全所属主题和动作、角色或结果语义：`service/apis/item-load-detail.ts`，不写脱离主题看不出归属的 `load-detail.ts`。
